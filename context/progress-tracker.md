# Progress Tracker

Update this file whenever the current phase, active feature, or meaningful implementation state change.

## Current Phase

- In Progress

## Current Goal

- Feature 30 (conversational AI Architect) implemented. Ready for next feature.

## Completed

- **Feature 01: Design System** — shadcn/ui (base-nova preset) initialized with Tailwind v4; Button, Card, Dialog, Input, Tabs, Textarea, ScrollArea components added to `src/components/ui/`; `src/lib/utils.ts` with `cn()` created; lucide-react installed; globals.css rewritten with project dark theme CSS variables mapped via `@theme inline`; layout.tsx updated to Inter + JetBrains Mono fonts.
- **Feature 02: Editor Chrome** — `src/components/editor/editor-navbar.tsx` (fixed navbar, sidebar toggle with `PanelLeftOpen`/`PanelLeftClose`, left/center/right sections, dark bg + bottom border); `src/components/editor/project-sidebar.tsx` (fixed overlay, slides in from left, My Projects/Shared tabs with empty states, New Project button); shadcn Dialog already available for future dialog patterns.
- **Feature 03: Authentication** — `@clerk/ui` installed; `src/proxy.ts` (protected-first `clerkMiddleware`, public routes from `NEXT_PUBLIC_CLERK_SIGN_IN_URL`/`NEXT_PUBLIC_CLERK_SIGN_UP_URL` env vars); `ClerkProvider` with `dark` theme and CSS variable overrides wraps root layout; `/` redirects authenticated → `/editor`, unauthenticated → `/sign-in`; `src/app/sign-in/[[...sign-in]]/page.tsx` and `src/app/sign-up/[[...sign-up]]/page.tsx` with two-panel desktop layout (left: logo + tagline + feature list, right: Clerk form), form-only on mobile; `UserButton` in editor navbar right section.
- **Feature 04: Project Dialogs** — `src/types/project.ts` (Project interface + MOCK_PROJECTS); `src/hooks/use-project-dialogs.ts` (dialog/form/loading state, slug derivation); `src/context/project-actions.tsx` (ProjectActionsContext + useProjectActions hook); `src/components/editor/project-dialogs.tsx` (Create/Rename/Delete dialogs); editor home screen in `src/app/editor/page.tsx` (heading + description + New Project button); sidebar updated with project list, per-item rename/delete actions (owned only), mobile backdrop scrim; EditorShell wires context + dialogs.
- **Feature 05: Prisma Data Models** — `prisma/models/project.prisma` (Project model with ownerId/name/description/status enum DRAFT|ARCHIVED/canvasJsonPath/timestamps + indexes on ownerId and createdAt; ProjectCollaborator model with cascade-delete relation, unique on projectId+email, indexes on email and projectId+createdAt); `prisma/schema.prisma` updated (fixed output path typo, removed url field — datasource URL now owned by `prisma.config.ts`); `src/lib/prisma.ts` cached singleton branching on DATABASE_URL prefix (prisma+postgres:// → accelerateUrl, otherwise PrismaPg adapter); migration `20260506100955_init_project_models` applied; Prisma client generated to `src/app/generated/prisma`.
- **Feature 06: Project API Routes** — `src/app/api/projects/route.ts` (GET lists owner's projects ordered by createdAt desc; POST creates project with optional name defaulting to "Untitled Project"); `src/app/api/projects/[projectId]/route.ts` (PATCH renames project — owner-only, 400 on blank name; DELETE removes project — owner-only); all routes return 401 for unauthenticated requests, 403 for non-owner mutations; Clerk `auth()` used for identity; no UI wiring.
- **Feature 07: Wire Editor Home** — `src/lib/projects.ts` (server helper `getEditorProjects` fetches owned projects by userId and shared via ProjectCollaborator email lookup using `currentUser()`); `src/types/project.ts` (simplified to `{ id, name, isOwned }`, MOCK_PROJECTS removed); `src/hooks/use-project-actions.ts` (unified hook: create calls POST and navigates to new workspace, rename calls PATCH and refreshes, delete calls DELETE and redirects to /editor if active project else refreshes; create dialog generates short suffix for room ID preview); `src/app/editor/layout.tsx` (converted to async server component; fetches projects and passes to EditorShell); `src/components/editor/editor-shell.tsx` (now uses `useProjectActions`, accepts `ownedProjects`/`sharedProjects` props, passes to sidebar); `src/components/editor/project-sidebar.tsx` (accepts real project lists as props, MOCK_PROJECTS removed); `src/components/editor/project-dialogs.tsx` (typed against `useProjectActions`, create dialog shows "Room ID:" preview, rename dialog no longer requires slug); `src/components/editor/home-new-project-button.tsx` (new client component for the New Project button); `src/app/editor/page.tsx` (converted to server component).
- **Feature 08: Editor Workspace Shell** — dynamic route renamed `src/app/editor/[slug]` → `src/app/editor/[roomId]`; `src/lib/project-access.ts` (`getClerkIdentity` returns `{ userId, primaryEmail }` via `currentUser()`; `findProjectByRoomId` matches slug or id; `userCanAccessProject` checks owner or `ProjectCollaborator` row by primary email); `src/app/editor/[roomId]/page.tsx` (server component: redirects unauthenticated users to `/sign-in`, renders `AccessDenied` for missing or unauthorized projects, otherwise renders `WorkspaceCanvas`); `src/components/editor/access-denied.tsx` (centered layout, lock icon, message, link back to `/editor` via `buttonVariants`); `src/components/editor/workspace-canvas.tsx` (full-viewport canvas placeholder with dark `bg-base` and centered "Canvas coming soon" message); `src/components/editor/ai-sidebar.tsx` (right-side floating overlay slide-in, header + sparkles empty state); `src/components/editor/editor-navbar.tsx` (optional `workspace` prop swaps logo for project name in center and adds Share button + AI toggle on the right); `src/components/editor/editor-shell.tsx` (reads `roomId` from `useParams`, finds current project from owned/shared lists, manages AI sidebar open state, passes `workspace` to navbar, `currentProjectId` to sidebar, mounts `AiSidebar` only when in workspace); `src/components/editor/project-sidebar.tsx` (`ProjectItem` accepts `isActive` — active item gets `bg-accent-dim`, brand folder icon, primary text, `aria-current="page"`); `src/hooks/use-project-actions.ts` (delete-active match now reads `params?.roomId` and compares against both `slug` and `id`).
- **Feature 09: Share Dialog** — `src/app/api/projects/[projectId]/collaborators/route.ts` (GET lists collaborators enriched via Clerk `getUserList({ emailAddress })` with `{ email, name, avatarUrl }`, returns `{ collaborators, isOwner }`; POST invites by email — owner only, validates format, checks for duplicates, returns enriched record); `src/app/api/projects/[projectId]/collaborators/[email]/route.ts` (DELETE removes collaborator — owner only, decodes URI email param); `src/components/editor/share-dialog.tsx` (Dialog fetches collaborators on open; copy-link button copies project URL with 2 s "Copied!" feedback; owner invite form with inline error; collaborator list with `CollaboratorAvatar` showing Clerk image or initials fallback, per-row remove button for owners; collaborators see read-only list); `src/components/editor/editor-shell.tsx` (adds `isShareOpen` state, `onShare` now calls `setIsShareOpen(true)`, mounts `ShareDialog` when `currentProject` is set).

- **Feature 10: Liveblocks Setup** — `liveblocks.config.ts` defines typed presence (`cursor`, `isThinking`) and user metadata (`displayName`, `avatarUrl`, `cursorColor`); `src/lib/liveblocks.ts` adds a cached Liveblocks Node client, deterministic cursor color helper, and private room creation via `getOrCreateRoom`; `src/lib/project-access.ts` exposes Clerk display name/avatar metadata through the existing identity helper; `src/app/api/liveblocks-auth/route.ts` adds the Node runtime `POST /api/liveblocks-auth` route that requires Clerk authentication, verifies project membership, enforces project ID as the room ID, creates the room if needed, and returns a room-scoped Liveblocks session token.
- **Feature 11: Base Canvas** — `src/types/canvas.ts` defines shared canvas node/edge types, `canvasNode`/`canvasEdge` type constants, the node color palette, and supported node shapes; `liveblocks.config.ts` now types the optional React Flow storage tree; `src/components/editor/workspace-canvas.tsx` is a client Liveblocks wrapper using `/api/liveblocks-auth`, `RoomProvider`, initial presence, `ClientSideSuspense`, and a connection error fallback; `src/components/editor/base-canvas.tsx` renders the Liveblocks-synced React Flow surface with empty initial nodes/edges, loose connections, `fitView`, `MiniMap`, and dotted background; `src/app/editor/[roomId]/page.tsx` remains server-side and passes the canonical project ID as the room ID.
- **Feature 12: Shape Panel** — `src/components/editor/shape-panel.tsx` adds a floating pill-shaped toolbar at the bottom-center of the canvas with draggable icons for all six shapes (rectangle, diamond, circle, pill, cylinder, hexagon); each drag sets `application/canvas-shape` data with the shape name and default dimensions; `src/components/editor/canvas-node.tsx` provides the `CanvasNodeRenderer` custom node component that renders a simple bordered rectangle with the label centered; `src/components/editor/base-canvas.tsx` split into `BaseCanvasContent` (uses `useReactFlow` for `screenToFlowPosition`, registers `nodeTypes`, handles `onDragOver`/`onDrop`, adds nodes via `onNodesChange([{ type: "add", item }])`) and `BaseCanvas` (wraps with `ReactFlowProvider`); node IDs generated from shape name, timestamp, and a module-level counter.
- **Feature 13: Node Shapes** — `src/components/editor/canvas-node.tsx` replaced with proper per-shape rendering: rectangle/pill/circle use CSS `border` + `borderRadius` (6px, 9999px, 9999px respectively); diamond/hexagon/cylinder render inline SVGs sized to the node's `width`/`height` props with `<polygon>` and `<ellipse>` elements; border color switches from `var(--border-default)` at rest to `var(--accent-primary)` when `selected`; SVG text labels use the node's color-pair text color; `src/components/editor/shape-panel.tsx` gained a drag ghost preview: `onDragStart` suppresses the browser's default ghost via a 1×1 canvas, sets `isDragging` state, and registers `dragover`/`dragend`/`drop` document listeners via `useEffect([isDragging])`; a fixed-position `PreviewShape` matching the dragged shape is portalled to `document.body` at 65% opacity and centered on the cursor; preview is removed on drop or dragend.
- **Feature 14: Node Editing** — `src/components/editor/canvas-actions-context.tsx` (new) defines `CanvasActionsContext` with `updateNodeLabel(id, label)` and exports `CanvasActionsProvider` / `useCanvasActions`; `src/components/editor/base-canvas.tsx` creates `updateNodeLabel` via `useCallback` dispatching a `{ type: "replace" }` `NodeChange` through `onNodesChange` (which Liveblocks handles to reconcile node data in storage), and wraps `ReactFlow` with `CanvasActionsProvider`; `src/components/editor/canvas-node.tsx` gains `NodeResizer` (visible when selected, 8×8 subtle dark handles, min 60×40px) and a `LabelEditor` overlay component — double-clicking a node enters edit mode, rendering an absolutely-positioned flex overlay that centers a transparent `<textarea>` over the node; the textarea auto-focuses and selects text on mount; blur or Escape closes editing; Enter commits without newline; all mouse/pointer events inside the editor stop propagation to prevent canvas drag/pan; SVG shapes hide their text label during editing; empty nodes display "Label" placeholder at 0.4 opacity.
- **Feature 15: Node Color Toolbar** — `src/components/editor/canvas-actions-context.tsx` extended with `updateNodeColor(nodeId, fill)` action (imports `CanvasNodeColor` type); `src/components/editor/base-canvas.tsx` implements `updateNodeColor` via `useCallback` dispatching a `{ type: "replace" }` node change updating `data.color`, passed into `CanvasActionsProvider`; `src/components/editor/canvas-node.tsx` gains `SwatchButton` (16px circle swatch with active border using paired text color, hover glow via box-shadow at 33% text-color opacity, propagation stopped on all pointer events) and `ColorToolbar` (pill-shaped floating toolbar anchored `top: -8px; transform: translate(-50%, -100%)` above the node, renders all 8 `NODE_COLORS` swatches, z-index 20); toolbar shown in both SVG and CSS shape render branches when `selected && !editing`; swatch click calls `updateNodeColor` updating `data.color`; text color is derived from the fill via existing `getTextColor` lookup so both update atomically.
- **Feature 16: Edge Behavior** — `src/types/canvas.ts` updates `CanvasEdgeData` from `Record<string, never>` to `interface CanvasEdgeData extends Record<string, unknown> { label?: string }`; `src/components/editor/canvas-actions-context.tsx` gains `updateEdgeLabel(edgeId, label)` action; `src/components/editor/canvas-edge.tsx` (new) is the custom `CanvasEdgeRenderer` — uses `getSmoothStepPath` for right-angle routing, renders a 20px transparent hit path for easy hovering/clicking, a visible 1.5px path with `transition: stroke` dimmed at rest (`#475569`) and brightened on hover/select (`#94a3b8`), dynamic `markerEnd` switching between two document-level SVG arrow markers, and `EdgeLabelRenderer` positioned at the `getSmoothStepPath` midpoint coordinates with a `LabelInput` (grows with content) on double-click, pill badge for saved labels, faint hint text when active with no label, and full stopPropagation to prevent canvas drag/pan; `src/components/editor/canvas-node.tsx` handle array changed to four `type="source"` handles with ids `top/right/bottom/left`; `src/components/editor/base-canvas.tsx` registers `edgeTypes`, sets `defaultEdgeOptions = { type: CANVAS_EDGE_TYPE }`, injects a hidden SVG with two arrow marker `<defs>` (`canvas-edge-arrow-rest`/`canvas-edge-arrow-active`), implements `updateEdgeLabel` via `EdgeReplaceChange` through `onEdgesChange`, and passes it into `CanvasActionsProvider`; `src/app/globals.css` handle CSS gains explicit `width: 8px; height: 8px; border-radius: 50%; background: #ffffff; border: 2px solid #1C2532` for white dot appearance.
- **Feature 17: Canvas Ergonomics** — `src/hooks/useKeyboardShortcuts.ts` (new) listens on `window` for `+`/`=` (zoom in), `-` (zoom out), `Cmd/Ctrl+Z` (undo), `Cmd/Ctrl+Shift+Z` and `Cmd/Ctrl+Y` (redo); skips shortcuts when focus is on an input, textarea, select, or contentEditable element; receives the React Flow instance plus undo/redo handlers as arguments; `src/components/editor/control-bar.tsx` (new) renders a pill-shaped floating toolbar with zoom-out / fit-view / zoom-in buttons, a thin vertical divider, and undo / redo buttons; disabled history buttons are visually dimmed via `opacity-30`; zoom actions call the React Flow instance with a 200ms duration; `src/components/editor/base-canvas.tsx` imports `useUndo`/`useRedo`/`useCanUndo`/`useCanRedo` from `@liveblocks/react`, calls `useKeyboardShortcuts`, and mounts `ControlBar` in a `bottom-left` React Flow `Panel` above the shape panel.
- **Feature 18: Starter Templates** — `src/components/editor/starter-templates.ts` (new) defines `CanvasTemplate` plus `CANVAS_TEMPLATES` (Microservices Architecture, CI/CD Pipeline, Event-Driven System) using shared canvas types; `buildNode`/`buildEdge`/`buildTemplate` helpers prefix template ids onto node/edge ids and apply per-shape default dimensions; `src/components/editor/starter-templates-modal.tsx` (new) is a Dialog with a scrollable two-column grid of `TemplateCard`s, each rendering a fixed 280×140 SVG `TemplatePreview` with bounds-fit scaling, plain lines between node centers, and shape-aware fills/strokes derived from `NODE_COLORS`; `src/components/editor/starter-templates-context.tsx` (new) provides `StarterTemplatesProvider`/`useStarterTemplates` for the open/close state shared across navbar and canvas; `src/components/editor/editor-shell.tsx` wraps content in `StarterTemplatesProvider` (split into `EditorShell` outer + `EditorShellInner`) and passes `onOpenStarterTemplates` through the workspace prop; `src/components/editor/editor-navbar.tsx` adds a `LayoutTemplate` "Templates" button next to Share when in workspace mode; `src/components/editor/base-canvas.tsx` consumes `useStarterTemplates`, implements `importTemplate` which dispatches a single combined `[remove..., add...]` change list to `onEdgesChange` and `onNodesChange` (replacing the canvas through the existing collaborative state) and calls `flow.fitView({ duration: 400, padding: 0.2 })` on the next frame, and mounts `StarterTemplatesModal` inside `CanvasActionsProvider`.
- **Feature 19: Presence Avatars & Live Cursors** — `liveblocks.config.ts` presence field renamed from `isThinking` to `thinking`; `src/components/editor/workspace-canvas.tsx` `initialPresence` updated accordingly; `src/components/editor/presence-avatars.tsx` (new) renders a frosted-glass pill in the top-right corner of the canvas with up to five overlapping collaborator avatars (profile image or initials fallback, `cursorColor`-tinted background, 2px surface ring for overlap separation), a +N overflow chip, a vertical divider that only appears when collaborators exist, and the Clerk `UserButton` at the same 32px size — all driven by `useOthers` with a `shallow` selector; `src/components/editor/base-canvas.tsx` imports `Cursors` from `@liveblocks/react-flow` and `useMyPresence` from `@liveblocks/react`, adds `onMouseMove` and `onMouseLeave` handlers to the `ReactFlow` component that broadcast cursor position in flow coordinates via `screenToFlowPosition`, renders `<Cursors />` inside `ReactFlow` for other participants' live cursors, and mounts `<PresenceAvatars />` in a `Panel position="top-right"`; `@liveblocks/react-ui/styles.css` added alongside the existing react-flow styles import.

- **Feature 20: AI Sidebar Shell** — `src/components/editor/ai-sidebar.tsx` rewritten as a full floating chat sidebar: header with Bot icon, "AI Workspace" title, and "Collaborate with Phantom AI" subtitle; shadcn `Tabs` with "AI Architect" and "Specs" tabs styled with `data-active:bg-elevated data-active:text-ai-text` for active state and `text-copy-muted` for inactive; AI Architect tab has a scrollable chat area with bot-icon empty state, three starter-prompt chips (`bg-subtle text-ai-text`), user messages (`bg-accent-dim border-2 border-brand/50 text-copy-primary` right-aligned) and assistant messages (`bg-elevated border-surface-border text-ai-text` left-aligned), auto-resizing textarea (72px min / 160px max), Enter-to-send / Shift+Enter newline, and a send button (`bg-brand text-white`); Specs tab has a Generate Spec button (`bg-brand text-white`) and a static demo spec card (`bg-elevated border-surface-border`) with FileText icon, title, snippet, and disabled Download action; overall sidebar uses `bg-base/95 backdrop-blur-sm` surface with existing slide-in behavior preserved.

- **Feature 21: Canvas Autosave** — `src/app/api/projects/[projectId]/canvas/route.ts` (new, Node runtime) implements `PUT` (validates JSON body, uploads to Vercel Blob at `canvas/{projectId}.json` with `addRandomSuffix: false, allowOverwrite: true, contentType: "application/json"`, then persists the returned blob URL to `Project.canvasJsonPath` via Prisma) and `GET` (reads `canvasJsonPath` from Prisma, returns `null` when unset, otherwise fetches the blob with `cache: "no-store"` and returns the parsed `{ nodes, edges }` JSON); both routes auth with `getClerkIdentity` + `findProjectByRoomId` + `userCanAccessProject` (401/403/404 response shape matches existing routes); reuses existing `Project.canvasJsonPath` schema field — no migration needed. `src/components/editor/canvas-save-context.tsx` (new) defines `CanvasSaveStatus` (`idle | saving | saved | error`) and a `CanvasSaveProvider` exposing `{ status, lastSavedAt, setStatus, registerSave, manualSave }` via `useCanvasSave`; `manualSave` calls a save function registered through `registerSave` (ref-based, set by the canvas hook). `src/hooks/use-canvas-autosave.ts` (new) debounces saves on `nodes`/`edges` change (1500 ms), aborts in-flight requests on rapid edits via an `AbortController`, skips the first effect run after `enabled` flips on (so loaded state isn't re-saved), updates the shared context status to `saving`/`saved`/`error`, and registers `performSave` on the context for the navbar's Save button. `src/components/editor/base-canvas.tsx` accepts a `projectId` prop, on mount fetches `GET /api/projects/{projectId}/canvas` only when the Liveblocks room has zero nodes/edges (initial-empty captured into a ref so re-renders don't retrigger the load), applies saved nodes/edges through `onNodesChange`/`onEdgesChange` add changes, then flips `loadComplete` to true to enable autosave; load failures fall back to an empty canvas. `src/components/editor/workspace-canvas.tsx` forwards `roomId` as `projectId` to `BaseCanvas`. `src/components/editor/editor-shell.tsx` wraps content in `CanvasSaveProvider` (alongside `StarterTemplatesProvider`) and threads `saveStatus` + `onSave` into the workspace prop. `src/components/editor/editor-navbar.tsx` adds a Save button between Templates and Share with a `SaveStatusContent` switch — `Save` icon at idle, spinning `Loader2` while saving, success-green `Check` once saved, error-red `AlertCircle` on failure (label updates to "Save"/"Saving"/"Saved"/"Retry save"); button is disabled while saving and triggers `manualSave` otherwise.

- **Feature 22: Design Agent API** — `prisma/models/task-run.prisma` (new) defines `TaskRun` (`id` cuid, unique `runId`, `projectId`, `userId`, `createdAt`) with index on `runId` and compound index on `userId, projectId`; migration `20260508000000_add_task_run/migration.sql` creates the `task_runs` table and indexes. `src/trigger/design-agent.ts` (new) exports `designAgentTask` (id `design-agent`) — minimal task accepting `{ prompt, roomId }`, logs receipt via `logger.info`, echoes payload back; no AI logic. `src/app/api/ai/design/route.ts` (new, Node runtime) `POST` validates `{ prompt, roomId, projectId }`, auths via `getClerkIdentity`, verifies project access via `findProjectByRoomId` + `userCanAccessProject`, triggers `design-agent` via `tasks.trigger<typeof designAgentTask>` with `{ prompt, roomId }`, persists the returned run id into `TaskRun` with `projectId`/`userId`, and returns `{ runId }` with 201; 401 unauthenticated, 400 missing fields, 403 non-member, 404 project not found. `src/app/api/ai/design/token/route.ts` (new, Node runtime) `POST` validates `{ runId }`, looks up the `TaskRun`, requires the requester's Clerk user id to match `taskRun.userId`, then calls `auth.createPublicToken({ scopes: { read: { runs: [runId] } }, expirationTime: "1h" })` and returns `{ token }`; 401 unauthenticated, 400 missing runId, 403 non-owner, 404 unknown run.

- **Feature 24: AI Presence State** — `src/types/tasks.ts` (new) defines `AI_STATUS_FEED_ID` constant, `AiTaskKind` (`design | spec`), `AiTaskStatus` (`started | processing | completed | failed`), and `AiStatusFeedPayload` (kind + status + optional `runId` + optional `text`); exports `isAiStatusFeedPayload` runtime guard and `isAiTaskActive` helper. `liveblocks.config.ts` adds `FeedMessageData: AiStatusFeedPayload` to the global `Liveblocks` interface so `useFeedMessages` is typed end-to-end. `src/components/editor/workspace-canvas.tsx` simplified to a `ClientSideSuspense` wrapper around `BaseCanvas` — `LiveblocksProvider`, `RoomProvider`, and the connection error boundary moved up to `editor-shell.tsx` so the AI sidebar can call room-scoped Liveblocks hooks; the `currentProject.id` now drives both the shell-level `RoomProvider` and the canvas, with the boundary keyed on it for clean reconnects between projects. `src/components/editor/base-canvas.tsx` `CollaboratorCursors` selector returns `thinking: o.presence.thinking === true`; the cursor name badge becomes a flex row that prepends a small `Loader2` spinner (`lb-cursor-spin` keyframes) when `thinking` is true and renders just the name otherwise. `src/components/editor/ai-sidebar.tsx` reads the shared status via `useLatestSharedAiStatus` (a `useFeedMessages(AI_STATUS_FEED_ID)` consumer that filters through `isAiStatusFeedPayload` and picks the highest-`createdAt` message), derives `sharedActive` via `isAiTaskActive`, and combines it with local busy state so the textarea + send button are disabled for everyone in the room while a generation is active; the send button swaps `Send` for an animated `Loader2` while busy. The sidebar lazily creates the feed via `useCreateFeed(AI_STATUS_FEED_ID)` (catching "already exists" errors) and posts payloads through `useCreateFeedMessage` at submit (`started`), once a run id arrives (`processing`), on each `useRealtimeRun` update (`processing`/`completed`/`failed`), and on submit-time errors (`failed`); `publishFeedStatus` retries once via `ensureFeed` if the first write fails. A new `SharedAiStatusStrip` between the header and tabs renders only the most recent valid feed payload as a small status pill (Sparkles/Loader2/Check/AlertCircle) with the optional `text` truncated next to it, leaving the rest of the sidebar fully usable.

- **Feature 25: Sidebar Chat Feed** — `src/types/tasks.ts` adds `AI_CHAT_FEED_ID = "ai-chat"`, a Zod-backed `aiChatMessageSchema` (`sender` 1–120 chars, `role` `"user" | "assistant"`, `content` 1–4000 chars, `timestamp` non-negative integer), an `AiChatMessage` interface that extends `Record<string, Json | undefined>` so it satisfies Liveblocks' `JsonObject`, an `isAiChatMessage` runtime guard wrapping `safeParse`, and a `FeedMessagePayload = AiStatusFeedPayload | AiChatMessage` union; `liveblocks.config.ts` `FeedMessageData` switched to `FeedMessagePayload` so `useFeedMessages` is typed for both feeds. `src/components/editor/ai-sidebar.tsx` adds a third "Chat" tab next to "AI Architect" and "Specs"; the new `RoomChatTab` reads via `useFeedMessages(AI_CHAT_FEED_ID)`, filters/sorts messages through `isAiChatMessage` + `createdAt`, derives the sender name from `useSelf().info.displayName` (falls back to `id`/`"Anonymous"`), renders each message with circular initials avatar, sender name, locale time stamp, and bubble (right-aligned `border-brand/50 bg-accent-dim` for self, left-aligned `border-surface-border bg-elevated` for others); send flow lazily creates the feed via `useCreateFeed(AI_CHAT_FEED_ID)` (catching "already exists" errors), posts the validated payload via `useCreateFeedMessage`, clears the input + resets the textarea height on success, and surfaces failures inline with an `AlertCircle` + message; textarea is disabled while sending and the send button swaps `Send` for an animated `Loader2`. `useLatestSharedAiStatus` continues to filter via `isAiStatusFeedPayload` so chat traffic and AI status traffic stay separated even though they share a typed FMD.

- **Feature 26: AI Chat Functionality** — `src/app/api/ai/design/route.ts` now also calls `triggerAuth.createPublicToken({ scopes: { read: { runs: [handle.id] } }, expirationTime: "1h" })` after triggering the run and returns `{ runId, publicToken }` in the same `201` response, collapsing the previous two-step trigger+token flow into one request. `src/components/editor/ai-sidebar.tsx` rewritten so the AI Architect tab reads/writes the `ai-chat` Liveblocks feed: shared `ChatMessageList` component renders entries from `useFeedMessages(AI_CHAT_FEED_ID)` filtered through `isAiChatMessage` and sorted by `createdAt`, with sender-name + timestamp header, sparkles avatar for assistant role, initials avatar for users, right-aligned `border-brand/50 bg-accent-dim` self bubbles and left-aligned `border-surface-border bg-elevated` other/assistant bubbles; on submit the user prompt is published to `ai-chat` (`role: "user"`, `sender: senderName`) via `useCreateFeedMessage`, then `POST /api/ai/design` is called with `{ prompt, roomId, projectId }` and `{ runId, publicToken }` are stored in local `activeRun` state; an inline `<ActiveRunSubscriber>` calls `useRealtimeRun<typeof designAgentTask>(runId, { accessToken: publicToken })` and dispatches `onProcessing`/`onCompleted`/`onFailed` once each (guarded by a `finishedRef`), with completion/failure pushing a final assistant message to `ai-chat` (`sender: "Phantom AI"`) and clearing `activeRun`; submit-time errors are surfaced as assistant messages in `ai-chat` per spec. Input disable state combines local `isSubmitting`/`activeRun` with `sharedActive` (any room participant's run) so the textarea + send button (which swaps `Send` for spinning `Loader2`) lock for everyone while a generation is in flight. New `ActiveStatusStrip` renders only when the latest `ai-status-feed` payload is in an active state (`isAiTaskActive`), sitting between the message list and the input as a compact `bg-base/60` bar with a `Loader2` "Starting"/"Working" pill and the truncated status `text`. Canvas updates remain entirely Liveblocks-driven — the sidebar never touches nodes/edges directly. The Chat tab still calls into the same `ChatMessageList` for rendering, so chat traffic between humans and AI uses the shared `ai-chat` feed end-to-end.

- **Feature 23: Design Agent Logic** — `liveblocks.config.ts` `RoomEvent` typed as a discriminated union of `{ kind: "ai-status", runId, status, message, at }` and `{ kind: "ai-presence", runId, cursor, thinking }`. `src/lib/ai/design-plan.ts` (new) defines a Zod `designPlanSchema` (`{ summary, actions[] }`) with a discriminated-union `designActionSchema` covering `addNode | moveNode | resizeNode | updateNodeData | deleteNode | addEdge | deleteEdge`, each shape/color enum derived from `NODE_SHAPES`/`NODE_COLORS`, plus a `DESIGN_AGENT_SYSTEM_PROMPT` codifying allowed shapes, color palette, layout grid (220px horizontal / 130px vertical), default node sizes (160×80, 180×90 for stores/queues), edge id format, and shape conventions (rectangle=service, pill=gateway, circle=client, cylinder=db, hexagon=queue, diamond=router). `src/trigger/design-agent.ts` rewritten — uses `createOpenRouter({ apiKey: OPENROUTER_API_KEY })` + `generateObject({ model: openrouter.chat("tencent/hy3-preview:free"), schema: designPlanSchema, system, prompt })` to plan; reads existing canvas via `liveblocks.getStorageDocument(roomId, "json")` and feeds the current nodes/edges as context to avoid id collisions; applies each action through `mutateFlow<CanvasNode, CanvasEdge>({ client, roomId }, flow => ...)` from `@liveblocks/react-flow/node` (calls `flow.addNode/updateNode/updateNodeData/removeNode/addEdge/removeEdge` per action, validating shape/color against the canvas types); broadcasts `ai-status` events at `started`, `processing`, `completed`/`failed` and updates Trigger.dev metadata (`status`, `message`, `summary`, `appliedActions`, `activity[]`); broadcasts `ai-presence` events with the AI cursor anchored at the center of each new/moved node and `thinking: true` while applying; final event clears cursor and thinking state on completion or failure; per-action errors are logged and skipped so a single bad action doesn't abort the whole run; outer try/catch surfaces errors as `failed` status without throwing past the task. `src/components/editor/ai-presence-overlay.tsx` (new) listens via `useEventListener` from `@liveblocks/react`, renders the AI cursor (purple `#A78BFA` arrow + Sparkles "Phantom AI" pill, transformed by React Flow `useStore` pan/zoom matching `CollaboratorCursors`) and a top-center status banner (purple/green/red badge variants for processing/completed/failed); banner auto-clears 6 s after the run completes or fails. `src/components/editor/base-canvas.tsx` mounts `<AiPresenceOverlay />` next to `<CollaboratorCursors />` inside `ReactFlow`. `src/components/editor/ai-sidebar.tsx` rewritten to call `POST /api/ai/design` then `POST /api/ai/design/token` on submit, store `{ runId, token, assistantMessageId }` as the active run, and render an inline `<ActiveRunSubscriber>` that uses `useRealtimeRun<typeof designAgentTask>(runId, { accessToken })` to mirror metadata `status`/`message`/`summary` into the assistant chat bubble (with status icon — Loader/Check/AlertCircle); textarea + send button disabled while a run is active. `src/components/editor/editor-shell.tsx` passes `currentProject.id` as both `projectId` and `roomId` into `AiSidebar`.

- **Bugfix: AI Workspace + AI Presence (current-issues.md)** — `src/components/editor/ai-sidebar.tsx`: both `AiArchitectTab` and `RoomChatTab` now eagerly create their feeds on mount via a `useEffect` (instead of lazily on first send), so `useFeedMessages` subscribes against an existing feed and writes don't race the feed-creation roundtrip. Silent `catch {}` blocks around `useCreateFeed`/`useCreateFeedMessage` were replaced with `console.warn` (suppressing only "already exists" via a new `isAlreadyExistsError` helper) so feed-write failures are debuggable. `publishChatMessage` now returns `boolean` and `handleSend` surfaces a write failure inline. Both tabs render an optimistic local user bubble (kept in `pendingMessages` keyed by `local-${ts}-${rand}`) immediately on submit and reconcile it out via a `useEffect` once the real feed message arrives — fixes the "I clicked send but nothing showed" symptom and makes the pre-existing chips/empty-state vs. real-message rendering work even when the feed write is slow. `AiArchitectTab` gains a `chatError` banner above the textarea (`AlertCircle` + `text-state-error`) mirroring the existing one in `RoomChatTab`. `src/trigger/design-agent.ts`: AI cursor sync rewritten — new `actionTargetCursor(action, trackedNodes)` derives the right target per action type (node center for `addNode`/`moveNode`/`updateNodeData`/`deleteNode`/`resizeNode`, midpoint of source/target centers for `addEdge`, no-op for `deleteEdge`); the per-action loop now broadcasts presence to that target BEFORE `mutateFlow` and awaits a `CURSOR_LEAD_MS = 220` lead so remote clients see the cursor travel to the spot first. The post-mutation `addNode/moveNode` broadcast was removed since the lead-broadcast already covers those, and edges/data-updates now drive the cursor too. `src/components/editor/base-canvas.tsx`: `<ReactFlow>` gains `fitViewOptions={{ padding: 0.2, maxZoom: 1 }}` so the queued initial `fitView` (which fires when the first AI-inserted node is measured on a previously-empty canvas) caps at 100% instead of zooming to the default 2× max. No node/edge rendering, navbar, or AI generation logic changed.

- **Feature 27: Spec Generation Flow** — `src/lib/ai/spec-schema.ts` (new) defines `generateSpecInputSchema` (Zod: `projectId`, `roomId`, `chatHistory` as `aiChatMessageSchema[]`, `nodes`/`edges` as permissive `.passthrough()` schemas capturing `id`/`position`/`width`/`height`/`data.{label,shape,color}` for nodes and `id`/`source`/`target`/`data.label` for edges) plus `SpecCanvasNode`/`SpecCanvasEdge` types. `src/trigger/generate-spec.ts` (new) exports `generateSpec` as a `schemaTask` (id `generate-spec`, `maxDuration: 300`) validated against `generateSpecInputSchema`; sets `metadata` status/message (`started` → `processing` → `completed`) for realtime tracking; calls `createOpenAI` + `generateText` with `gpt-4o-mini` and a `SPEC_GENERATION_SYSTEM_PROMPT` that turns the canvas nodes/edges + chat history into a structured Markdown spec (Overview/Components/Data Flow & Interactions/Design Notes); returns the Markdown string directly as task output (errors propagate/throw for Trigger.dev's normal retry/failure handling, unlike `design-agent`'s catch-and-return pattern). `src/app/api/ai/spec/route.ts` (new, Node runtime) `POST` validates `roomId` is present, auths via `getClerkIdentity`, resolves the project via `findProjectByRoomId(roomId)` + `userCanAccessProject` (401/404/403), validates `{ chatHistory, nodes, edges }` plus server-resolved `projectId`/`roomId` against `generateSpecInputSchema` (400 on failure — client-supplied `projectId` is ignored/overridden), triggers `generate-spec` via `tasks.trigger<typeof generateSpec>`, persists a `TaskRun` row, and returns `{ runId }` with 201. `src/app/api/ai/spec/token/route.ts` (new, Node runtime) mirrors `/api/ai/design/token` — `POST` validates `{ runId }`, looks up `TaskRun`, requires `taskRun.userId === identity.userId`, returns `auth.createPublicToken({ scopes: { read: { runs: [runId] } }, expirationTime: "1h" })` as `{ token }` (401/400/403/404). No frontend wiring — Specs tab UI remains the static demo card from Feature 20.

- **Bugfix: AI Workspace issues pass 2 (current-issues.md)** — Quick safe fixes:
  `src/components/editor/editor-shell.tsx` `isAiSidebarOpen` now defaults to `true`
  so the AI Workspace is open as soon as a workspace (incl. a freshly created
  project) loads, instead of requiring a toggle click (issue 1a). `src/lib/ai/design-plan.ts`
  `DESIGN_AGENT_SYSTEM_PROMPT` edge rules changed from "label only when it adds
  information / otherwise omit" to "every edge MUST carry a short meaningful label"
  so generated arrows are no longer empty (issue 2b); schema `label` kept optional to
  avoid hard generation failures. The larger conversational change (issue 2a/2b:
  discuss + query + incremental tweak) is specced in
  `context/feature-specs/28-ai-architect-conversational.md` and awaiting approval
  before implementation — it targets the **AI Architect** tab (the AI surface); the
  **Chat** tab stays human-to-human collaboration. Noted code drift:
  `src/trigger/design-agent.ts` uses OpenAI `gpt-4o-mini`, not the OpenRouter model
  recorded in Feature 23.

- **Feature: Spec Persistence & Download** (`context/feature-specs/28-spec-persistence-download.md`) — `prisma/models/project-spec.prisma` (new) defines `ProjectSpec` (`id` cuid, `projectId`, `filePath`, `createdAt`) with a cascade-delete relation to `Project` and index on `(projectId, createdAt)`; `prisma/models/project.prisma` adds the reverse `specs ProjectSpec[]` relation; migration `20260615000000_add_project_spec/migration.sql` creates the `project_specs` table, index, and FK (not yet applied — DB connection blocked per Session Notes). `src/trigger/generate-spec.ts` now, after generating the Markdown text, creates a `ProjectSpec` row (`projectId`, empty `filePath`), uploads the text to Vercel Blob at `specs/{projectId}/{specId}.md` (`access: "private"`, `addRandomSuffix: false`, `allowOverwrite: true`, `contentType: "text/markdown"` — same pattern as canvas persistence), updates the row's `filePath` to the blob URL, and sets `metadata.specId` alongside the existing `status`/`message` fields; task output (`text`) is unchanged. `src/app/api/projects/[projectId]/specs/[specId]/download/route.ts` (new, Node runtime) `GET` auths via `getClerkIdentity`, resolves the project via `findProjectByRoomId` + `userCanAccessProject` (401/403/404), looks up the `ProjectSpec` by `specId` and verifies `spec.projectId === project.id` (404 if mismatched/missing), fetches the Markdown from `spec.filePath`, and returns it as a `text/markdown` attachment (`Content-Disposition: attachment; filename="spec-{specId}.md"`).

- **Feature 29: Spec UI Integration** (`context/feature-specs/29-spec-ui-integration.md`) — `src/app/api/projects/[projectId]/specs/route.ts` (new, Node runtime) `GET` mirrors the auth/access pattern of the canvas and download routes (`getClerkIdentity` + `findProjectByRoomId` + `userCanAccessProject`, 401/404/403), then lists `ProjectSpec` rows for the project ordered by `createdAt desc` and returns `{ specs: [{ id, createdAt, filename }] }` where `filename` is derived as `spec-{id}.md` (matching the download route's `Content-Disposition`). `src/components/editor/specs-tab.tsx` (new) replaces the static demo card in the AI sidebar's Specs tab: `SpecsTab` fetches the list on mount, renders a compact scrollable (`ScrollArea`) list of `FileText`-icon rows showing `filename` + locale-formatted `createdAt` with a per-row download button, and an empty/loading/error state; clicking a row opens `SpecPreviewModal`, a shadcn `Dialog` that lazily fetches the spec's Markdown via the existing `GET /api/projects/{projectId}/specs/{specId}/download` endpoint (reused for preview — `fetch().text()` ignores the `Content-Disposition` header) and renders it with `react-markdown` (new dependency) using a custom `components` map styled with the project's `globals.css` tokens (`copy-primary`, `copy-secondary`, `copy-muted`, `ai-text`, `brand`, `surface-border`, `bg-subtle`). Keyboard support (Escape to close, focus trap) comes from the existing `@base-ui/react` Dialog primitive. Download (list item + modal) creates a temporary `<a download>` pointing at the download endpoint so the browser handles the file save — no Blob URLs touched client-side. `src/components/editor/ai-sidebar.tsx` imports `SpecsTab` and drops the now-unused `Download`/`FileText` icon imports. The "Generate Spec" button remains static/unwired per scope limits (generation flow is Feature 27/28's responsibility).

- **Feature 30: Conversational AI Architect** (`context/feature-specs/30-ai-architect-conversational.md`) — The AI Architect tab now supports three turn types (design / tweak / discuss-query) without changing the human Chat tab. `src/lib/ai/design-plan.ts` extracts a shared `CANVAS_DESIGN_RULES` constant (allowed shapes/colors, addNode/addEdge action formats, layout/edge/shape conventions) out of `DESIGN_AGENT_SYSTEM_PROMPT` so both agents stay in sync; `designActionSchema`/`designPlanSchema` unchanged. `src/lib/ai/canvas-apply.ts` (new) is the shared canvas-mutation module extracted from the old `design-agent.ts`: `OPENAI_MODEL_ID`, `VALID_SHAPES`/`VALID_COLORS`, `AiAgentStatus`, `broadcastStatus`/`broadcastPresence`/`pushStatus` (Trigger.dev `metadata` + Liveblocks `broadcastEvent`), `describeCanvas` (reads current nodes/edges via `getStorageDocument`), `buildNodeFromAction`/`buildEdgeFromAction`/`nodeCenter`/`actionTargetCursor` (with `CURSOR_LEAD_MS = 220` AI-cursor lead), and `applyDesignActions({ client, roomId, runId, actions, existingNodes })` which runs the full per-action `mutateFlow` loop (addNode/moveNode/resizeNode/updateNodeData/deleteNode/addEdge/deleteEdge) with cursor-lead presence broadcasts, per-action error logging/skipping, and `metadata.set("appliedActions", ...)`. `src/trigger/design-agent.ts` now just plans via `DESIGN_AGENT_SYSTEM_PROMPT`/`designPlanSchema` and calls `applyDesignActions` — behavior unchanged. `src/lib/ai/architect-plan.ts` (new) defines `architectPlanSchema = { reply: string, actions: designActionSchema[] (default []) }` and `ARCHITECT_SYSTEM_PROMPT`, which embeds `CANVAS_DESIGN_RULES` and instructs the model to classify each turn — **discuss/query** → empty `actions` + concise `reply`, no canvas change; **tweak** → smallest incremental `actions` set extending the existing canvas (never wipe/rebuild unless explicitly asked) + a `reply` describing the change; **design** → full addNode/addEdge generation (canvas empty or explicit from-scratch request) + a `reply`. `src/trigger/architect-agent.ts` (new) exports `architectAgentTask` (id `architect-agent`, payload `{ prompt, roomId, history?: AiChatMessage[] }`); mirrors `design-agent.ts`'s status/presence lifecycle (`pushStatus`/`broadcastPresence` at started/processing/completed/failed), builds a `canvasContext` from `describeCanvas` plus a `describeHistory(history)` summary of the last ~10 turns, calls `generateText` with `Output.object({ schema: architectPlanSchema })`, sets `metadata.reply`/`metadata.summary` to `plan.reply`, and only calls `applyDesignActions` when `plan.actions.length > 0`. `src/types/tasks.ts` adds `AI_ARCHITECT_FEED_ID = "ai-architect"` and extends `AiTaskKind` to `"design" | "spec" | "architect"`. `src/app/api/ai/architect/route.ts` (new, Node runtime) mirrors `/api/ai/design/route.ts`'s auth/validation (401/400/403/404) but additionally accepts `history` (filtered through `isAiChatMessage`, capped to last 10 via `HISTORY_LIMIT`), triggers `architect-agent`, persists a `TaskRun`, and returns `{ runId, publicToken }`. `src/components/editor/ai-sidebar.tsx`: `ActiveRunSubscriber` is now generic (`<TTask extends AnyTask>`, from `@trigger.dev/core/v3`) so it can subscribe to either `designAgentTask` or `architectAgentTask` via explicit JSX type args; `AgentRunMetadata` (renamed from `DesignAgentRunMetadata`) adds `reply?: string | null`, and completion reads `md.summary || md.reply || md.message`. A new root-level `useEnsureAiFeeds()` hook (mounted once in `AiSidebar`) creates `ai-chat`, `ai-architect`, and `ai-status-feed` exactly once per session, replacing the old per-tab mount-time `createFeed` effects (fixes the duplicate-`createFeed` "Feed mutation timeout" race). `AiArchitectTab` now reads/writes the separate `ai-architect` feed (`publishArchitectMessage`, with `ensureArchitectFeed` retry-on-failure), builds `history` from the last `HISTORY_LIMIT = 10` entries before sending, and always calls `POST /api/ai/architect` with `{ prompt, roomId, projectId, history }` — the architect agent itself decides design vs. tweak vs. discuss; on completion it posts `md.reply`/`md.summary` back to `ai-architect` via `<ActiveRunSubscriber<typeof architectAgentTask>>`. All `publishStatus` payloads in this tab now use `kind: "architect"`. `RoomChatTab` (human Chat tab, `ai-chat` feed) is otherwise unchanged aside from removing its now-redundant mount-time `ensureChatFeed` effect (feed creation moved to `useEnsureAiFeeds()`); its retry-on-write-failure `ensureChatFeed` helper is retained. `npm run build` passes with no TypeScript errors.

- **Bugfix: current-issues.md (3 issues)** —
  **Issue 1 (AI Architect reply missing):** `src/components/editor/active-run-subscriber.tsx` (new) extracts `ActiveRunSubscriber<TTask>` / `AgentRunMetadata` into a shared file to avoid circular imports. `src/components/editor/ai-sidebar.tsx`: `handleCompleted` and `handleFailed` now optimistically prepend the assistant reply to `pendingMessages` before clearing `activeRun` (so the chat never goes silent) and pass a deterministic `id` (`architect-reply-{runId}` / `architect-failed-{runId}`) to `createFeedMessage` so the eventual server-reconciled feed message dedupes with the optimistic entry. `publishArchitectMessage` returns `boolean` and surfaces write failures via `chatError`. **Issue 2 (Generate Spec button unwired):** `src/components/editor/specs-tab.tsx`: `SpecsTabProps` gains `roomId`; `fetchSpecs` is extracted as a `useCallback` (reused on mount and after generation); `handleGenerateSpec` POSTs to `/api/ai/spec` then `/api/ai/spec/token`, sets `activeSpecRun`, and shows live `genStatusText`; `handleSpecCompleted` calls `fetchSpecs()` so the new spec appears without a tab re-open; `handleSpecFailed` surfaces the error inline; button shows `Sparkles` at idle and `Loader2` while generating; `<ActiveRunSubscriber<typeof generateSpec>>` is rendered conditionally; canvas nodes/edges are read via `useLiveblocksFlow<CanvasNode, CanvasEdge>()`; chat history is read from `useFeedMessages(AI_ARCHITECT_FEED_ID)`. **Issue 3 (overlapping canvas layouts):** `src/lib/ai/canvas-apply.ts` adds `resolvePosition(candidate, w, h, occupied)` — iteratively nudges a new node right past the first conflicting occupied bbox (with 24px padding) up to 30 iterations, then drops below all nodes as fallback; `ApplyActionsParams` gains optional `existingEdges?: CanvasEdge[]`; `applyDesignActions` applies `resolvePosition` before each `addNode` and `moveNode` mutation so coordinates are collision-free before hitting Liveblocks. `src/trigger/design-agent.ts` and `src/trigger/architect-agent.ts` now pass `existingEdges` to `applyDesignActions`. `src/lib/ai/design-plan.ts` `CANVAS_DESIGN_RULES` layout section strengthened: explicit "CRITICAL — no overlapping" rule, "check all other nodes before assigning a position", "when adding to non-empty canvas find rightmost x and offset by ≥ (width + 220)". `src/lib/ai/architect-plan.ts` action rules gain a "Tweak placement" rule: read max x/y from context, never default to (0,0) or reuse an existing coordinate. `npm run build` passes.

## In Progress

- [Nothing in progress]

## Next Up

- Apply migration `20260615000000_add_project_spec` once the Prisma Postgres connection issue is resolved.
- Spec's open question: consider an explicit "design from scratch" affordance that calls `/api/ai/design` directly even when the canvas is non-empty (currently the architect agent decides design vs. tweak vs. discuss itself based on canvas state + prompt wording).

## Open Questions

- [Any unresolved product or technical decisions]

## Architecture Decisions

[update as we go]

- shadcn/ui v4.7.0 uses "base-nova" preset (the replacement for "new-york" style in the new CLI). Components are generated from @base-ui/react primitives.
- Tailwind v4 CSS-first config — no tailwind.config.js. All tokens defined in globals.css via `@theme inline`.
- Dark-only theme: all shadcn `--background`, `--foreground`, etc. are set in `:root` to dark values; no `.dark` class toggling needed.
- `dark:` variant classes on generated shadcn components are inert (class never applied) but harmless since `:root` already provides dark values.

## Session Notes

[update as we go]

- Fixed Prisma Postgres runtime configuration to use the pooled `DATABASE_URL`, added `DIRECT_URL` for Prisma CLI commands, and added migration `20260507000000_add_project_slug` for the project slug column used by editor room URLs. Applying/verifying the migration is blocked because both Prisma Postgres TCP endpoints currently reset connections with `ECONNRESET` / Prisma `P1017`.
- Project uses Next.js 16.2.4 with Tailwind v4 (`@import "tailwindcss"` syntax, no tailwind.config.js).
- shadcn CLI v4.7.0 — `--style` flag removed; use `--preset` or `-d` for defaults.
- `npx shadcn@latest init -d` works non-interactively (uses next template + base-nova preset).
- `npx shadcn@latest add <components> --yes` adds components without prompts.
- Do not modify generated `src/components/ui/*` files.
- README initialized with project overview, current implementation status, setup steps, environment variable names, scripts, and repository structure.
- Feature 10 required adding `@liveblocks/node` to match the already-declared Liveblocks client packages; `npm install @liveblocks/node@^3.19.0` also reconciled `package-lock.json` for Liveblocks/React Flow dependencies listed in `package.json`.
- README setup docs now include the required `LIVEBLOCKS_SECRET_KEY` environment variable.
